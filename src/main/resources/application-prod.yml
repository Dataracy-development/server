spring:
  datasource:
    url: jdbc:mysql://dataracy-db.cromecsg8k2g.ap-northeast-2.rds.amazonaws.com:3306/dataracy-db
    username: admin
    password: dataracyPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  kafka:
    bootstrap-servers: kafka:9092

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080

logging:
  level:
    root: info

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      show-details: always
