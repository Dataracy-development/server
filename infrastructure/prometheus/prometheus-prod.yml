global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'dataracy-backend-prod'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['backend-blue:8081']  # 운영에서 현재 활성 컨테이너 이름
